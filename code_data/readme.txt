Data: 
11 persons, ADL(Activity of Daily Life, labelled as 0)  and Fall Data (labelled as 1-4)

original data: data (directory)

transformed features: features (directory)

preprocessing code: preprocess.cpp
	input:  data (directory)
	output: features (directory)

fall_test.txt: is only combined with all files in "features" directory
	after preprocessing, the matrix size is 1004*474, which can be used for debugging PCA


